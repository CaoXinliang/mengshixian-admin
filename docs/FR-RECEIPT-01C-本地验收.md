# 简化企业线下收款：本地同单验收

## 本次实际操作

命令：`node test/import-browser.test.cjs --actual-receipts`。

本地合成商品/企业账号经实际业务API完成企业审核、地址、价格库存与配送准备，`orders.create`产生100元线下订单。隔离Edge订单列表读取同一应用/内存数据库：

1. 显示未收款，从该行打开“查看收款”。
2. 输入30元、北京时间、银行转账和本地测试备注，确认登记；返回实收30元/待收70元，流水1条。
3. 输入70元并确认，返回实收100元/待收0元/已收齐，流水2条。
4. 连续触发表单提交没有产生额外请求；重放第一笔凭据返回idempotent，实收仍100元。
5. 履约仍待确认，没有因为收齐而自动发货或送达。

截图：`outputs/receipt-browser/actual-api-paid.png`，主Agent已实际查看。真实订单号由本地业务函数生成，不是浏览器固定返回。全31测试文件及差异检查通过。

首次连通测试因调试连接未启用Runtime事件，页面没有收到API结果；启用后整条链路通过。没有改生产业务代码来绕过失败。

## 必须分开的结论

- 本地：业务函数+内存数据库+实际浏览器页面收款已通过，既有模拟用例另覆盖取消、响应中断、跨刷新恢复和跨单提示。
- 友方云环境：未部署、未创建order_receipts集合、未验证云事务并发，没有真实资金或业务数据写入。
- 实际小程序：只读确认有企业offline下单分支；“实付”文案及收款状态尚未接合，不能称实际小程序已完成。前端本轮未修改。
- 权限：演示用本地测试管理员；接口专项另测无权限拒绝，已有显式角色不会因代码默认角色改变而自动扩权。
- 会话恢复：当前标签页刷新可恢复；关闭标签页、清浏览器数据、换设备恢复未覆盖；结果不明须先核对，不可另记。

价格、商品、库存、身份均为明确本地测试数据，不代表甲方已确定商品价格或企业定价。其他运维整改剩余项不因收款成功而自动完成。

## 最终两轴审查

Standards：独立审查未见阻断，指出测试入口“全部mock”注释过期；主Agent已修正为默认模拟、指定开关实际业务API+内存存储。Spec：独立审查确认同一createApplication与同单实际流水，未见阻断。两位本轮未重跑命令，执行证据来自主Agent前述运行与截图。

结论：FR-RECEIPT-01A/B/C本地目标收口；不声称完整原样执行技能工具链，不代表云端/实际小程序完成，不代表整个运维整改目标完成。
