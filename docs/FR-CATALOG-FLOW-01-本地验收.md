# 交付表格与商品连续验收：当前证据

## 本地主路径收口与保留边界

独立需求审查确认业务调用未mock，顾客多身份共用同一内存资料；指出逐行原因缺断言，主Agent补合成错误表第2行缺名称、第4行重复、第3行缺价格/主图，以及交付6行逐行行号/缺项，完整浏览器复跑通过。补配送时段API持久化字段及同商品发布审计唯一记录/对象/操作者/时间断言亦通过。

仅本地主路径收口，FR-CATALOG-FLOW-01全部验收仍未完成：实际小程序/云存储/云事务、长视频、发布依赖新增记录并发等不因本次通过关闭。测试价格没有source/temporary标志，边界是内存隔离；企业资质占位仅用于本地身份测试，不是资质验收。按用户排队进入独立FR-STAFF-01A，不重做已完成流程。

## 最新：本地主链明确发布与顾客接口报价通过

主Agent完整命令`node test/import-browser.test.cjs --actual-catalog --actual-catalog-media --actual-catalog-ready`通过。新增`test/catalog-publish-browser.cjs`，在此前同一内存商品/规格/素材/价格/库存/配送上继续：

- 草稿时个人catalog.products不可见；页面取消发布后仍draft且不可见。
- 页面核对商品名称并明确确认（连续触发两次），实际业务API变为on_sale，个人目录及详情返回同一商品/规格，主图和视频关联齐全。
- 个人小程序价格1250分；无登录身份请求价格返回UNAUTHENTICATED。
- 同区域真实本地地址接口＋checkout.quote：2件商品2500分、运费500分、应付3000分；11件超过可售10件被拒绝。
- 另一独立本地身份经申请/管理员审核变成企业；默认all商品可见，但价格列表为空，企业报价返回PRICE_NOT_AVAILABLE，没有虚构企业价或沿用个人价。

顾客调用是fixture.customerCall创建的独立身份应用，共享同一内存数据库并调用真实dispatch，不是小程序UI。好友小程序只读核对services/catalog.js与checkout.js确实调用catalog.products/product/prices及checkout.quote；未修改、编译或运行小程序，不称实际小程序可见。

全35测试文件及差异检查通过。库存/价格/配送helper与库存反馈修复只读复核未见阻断；发布/身份段独立需求复核已派发、待返回。整个运维目标未完成：发布依赖新增记录并发边界、云端/长视频/真实小程序及排队工作人员任务仍需分别处理。

## 同商品价格、库存、配送到待确认发布

主Agent执行`node test/import-browser.test.cjs --actual-catalog --actual-catalog-media --actual-catalog-ready`通过，沿同一交付表商品：

- 价格页按商品/规格名称选择，仅个人顾客小程序价格12.50元；取消不建规则，确认后实际API保存1250分，无公开价或企业价。价格补齐仍因缺库存配送而禁止发布。
- 仓库页创建明确本地演示仓；库存页名称选择仓库/规格，取消不入库，确认后onHand=10/reserved=0/available=10。
- 配送区域按省市区名称选择、勾选仓库，新区域默认停用，明确启用；取消不保存。运费页按名称选区域与仓库，填5元基础运费及100元免运门槛；配送时段09:00–12:00，分类页启用导入分类。
- 核对页资料已齐、发布按钮可用；API仍draft，未点击发布。不把此步称上架、顾客可见或下单完成。

首次库存测试暴露helper选择器引号错误，修正后又复现真实产品bug：app.js库存接口成功后读取异步事件currentTarget.reset为空，显示失败且不刷新。改用已捕获的inventoryForm重置后整链通过；没有重复写同一真实库存，所有演示均为独立内存进程。新helper共pricing/inventory/delivery三个模块；库存表单反馈修复为唯一产品代码改动。独立复核已派发，尚待结果。

全部价格、库存、运费是显式本地合成测试输入，不是甲方确认资料；现有价格表单不写source/temporary，靠隔离内存边界保证不污染线上，不声称价格记录自带演示标志。下一动作明确发布、身份可见与实际报价的同数据验收。

## 素材连续验收准备

### 实际浏览器结果（准备后的最新状态）

主Agent执行`node test/import-browser.test.cjs --actual-catalog --actual-catalog-media`成功。同一交付TEST-S-0001生成的商品，跨到素材页：上传本地logo.png作为明确标记demo/temporary的测试主图、浏览器录制的短WebM作为同标记测试视频，经真实API上传及登记；回核对页按名称选择，保存主图与详情视频关联。图片naturalWidth>0，视频实际播放至currentTime>0.1。API读取两条关联及同一素材文件，缺价格时页面禁发布、直接调用发布API也返回PRODUCT_NOT_READY；商品仍draft。

仅证明本地短文件上传/登记/关联/显示与播放起步，不证明生产客户端分段传输、长视频完整播放或友方云存储。35测试文件和差异检查通过。媒体helper已交回；下一步两助手分别准备同商品价格与库存页面测试，不修改业务代码。

`test/support/catalog-flow-fixture.cjs`新增纯内存文件适配器，每次上传返回独立文件标识，预览解析为内存data URL；无CloudBase SDK或网络存储。`test/catalog-storage-fixture.test.js`先因缺存储返回MEDIA_UPLOAD_UNAVAILABLE失败，接入后验证实际上传不自动登记、登记不发布、重复上传不覆盖旧文件。全35测试文件通过。

浏览器实际API桥新增小文件传输适配及临时URL替身；不mock上传/登记/关联的业务回应。范围为≤4MB测试文件，不代表生产api-client分段传输或云存储可靠性。`--actual-catalog-media`开关准备接同商品图片/视频页面helper，助手独占`test/catalog-media-browser.cjs`编写；尚未运行这条新增浏览器链，不预报成功。

## 2026-09-23：交付文件到导入草稿

主Agent实际运行 `node test/import-browser.test.cjs --actual-catalog` 成功。独立无头Edge加载本机imports.html，两份既有交付XLSX经文件输入框的change流程与实际解析器处理；文件字节从本地预览服务读取，不是伪造解析结果。没有操作Windows文件选择对话框。

- 空白模板提示没有数据行，保存按钮不可用。
- 项目资料演示表预览6行；选择文件、取消确认均无导入记录。
- 确认后，实际业务API建立6条staged导入草稿；商品与规格记录仍为0，不自动上架。
- 再次确认、重新上传同文件均未增加草稿；重传显示6行重复、禁止保存。
- 数据库为隔离内存适配器，业务API未mock；云SDK/实际云数据未使用。不是友方云环境或小程序验收。

新增测试：`test/catalog-actual-api-browser.cjs`、`test/support/catalog-flow-fixture.cjs`；入口增加 `--actual-catalog`。收款夹具仅导出内存适配器，未改变收款行为。

## 快速换文件保护

只读助手指出A/B连续选择可能迟到覆盖。主Agent使用真实CSV解析器、实际业务API和可延迟响应复现失败：最后选B，预览却显示A。`admin-import-workflow.js`增加选择版本，旧解析、预览、错误及映射回调不能覆盖新选择；保存期间禁选文件。

`test/import-workflow-race.test.js`验证只保存B、保存期间不能切换C；先失败、修正后通过。32个前后台测试文件通过，差异检查通过；之后完整隔离浏览器验收亦通过。

## 开放项与下一动作

### 同一交付记录跨页核对（后续实测）

主Agent执行`node test/import-browser.test.cjs --actual-catalog`通过，新增`test/catalog-draft-review-browser.cjs`沿TEST-S-0001而非另造商品继续：取消“生成商品草稿”时商品仍0条，明确确认后商品及规格均为draft，点原行“核对发布”进入真实核对页；商品名称对应交付行，显示缺项，发布按钮禁用，API读取仍draft。此步未补素材、价格或库存，完整发布链路仍未完成。

收款测试等待已仅在`test/order-receipts-browser.cjs`修正：确认旧标记消失、新文档管理员身份就绪，刷新前后凭据相同，待核对弹窗实际打开，再断言禁止另记和同凭据重试。助手串行3次完整浏览器运行通过，主Agent接入上述跨页后再跑一次通过。未修改产品收款代码，未把本地模拟收款回归说成云端收款验证。

### 后续复测：编码映射等待与重复点击

`test/import-mapping-race.test.js`使用真实映射模块、解析器和本地API，复现核对未结束时旧的一条记录被保存（预期0，实际1）。工作流核对开始即清除可保存结果、禁用保存、隐藏旧预览；核对结束才恢复。随后复现映射按钮未禁用，并增加独立进行中守卫与按钮禁用，防止双击生成两套编码。两项均先失败再通过。

`test/catalog-actual-api-browser.cjs`增加浏览器实际接口响应延迟场景：等待映射时点击保存，原6草稿不增加；完成后保存2条，合计8条，仍没有商品/上架。主Agent已运行通过。全33个测试文件及差异检查通过。

首次整套浏览器运行在既有收款刷新检查`test/order-receipts-browser.cjs:113`失败（form.hidden为false），原样复测通过；期间仅修改导入映射，不是收款修复。已交助手只读排查刷新与初始化时序，原因未确认，不把重跑绿灯当问题消失。

1. 单次映射等待保存、重复点击及跨文件面板归属风险均已修复。`test/import-mapping-panel.test.js`先复现A返回隐藏B，再用每次挂载owner保护消除；最终只读复核相关段未见新阻断。34测试文件通过。收款刷新检查等待时序已修正并完成上述重复验证。
2. 同一批导入草稿→生成商品草稿→跨页关联素材、价格、库存、核对发布→身份可见性尚未接完。既有分散测试不代替完整链路。
3. 云存储、真实事务和实际测试小程序未验收；禁止部署/线上写入仍有效。

本轮未修改交付模板、个人线、小程序或回退点，没有提交、推送、部署。业务改动仅导入页面异步状态保护，保留蓝色多页面架构；本轮文件均小于800行。
