# FR-RECEIPT-01C：同单本地浏览器与实际API验收

依据：用户已采纳的第三步及规格v1；01A/01B本地门槛和两轴检查收口。唯一状态入口仍为OPERATIONS-USABILITY-PLAN.md。

完成条件：隔离浏览器调用同一createApplication实例及内存存储；测试企业身份通过orders.create创建线下订单，页面实际列表找到该单、打开收款、登记部分再登记剩余，重新读取真实业务函数返回的流水与订单。验证重复请求不增加流水、订单履约独立、真实admin.me字段及权限；不得使用固定成功响应替代收款业务。

测试价格、商品与账号明确为本地合成夹具，不使用云数据、不改生产价格。数据库与存储仍是本地替身；不证明真实CloudBase并发或小程序可见。以截图、命令结果、订单前后状态和剩余限制记录交付。

复用已有Edge驱动及API，不建第二套后台，不引入线上桥接。测试工具按模块分文件且均不超过800行。不推送提交部署、不写线上业务、不动个人线、小程序和原回退点。
