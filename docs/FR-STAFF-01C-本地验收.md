# FR-STAFF-01C 工作人员本地浏览器验收

状态：LOCAL_DONE（2026-09-23）。仅友方后台及必要友方API；无提交、推送、部署或线上业务写入。此前回退点`friend-admin-before-next-change-2026-09-23`保持不动，其他未提交修改保留。

## 运维可操作结果

超级管理员从“系统管理 → 账号与权限”新增工作人员，填登录账号、姓名、手机号、明确选择“超级管理员/运营”、初始密码；在列表按姓名/登录账号选人后可编辑、停用或启用、重置密码。每次先显示对象核对，再确认执行。页面显示只读账号ID、角色、脱敏手机号及“未验证”；不能把登记号码当作真实验证。运营进入同页只看本人资料，可改本人密码，无人员管理入口。22张受保护后台HTML页均加载公共顶栏，右上角显示身份和退出；两张独立商品页也在加载业务资料前校验实际会话。退出请求失败时提示重试并保留登录状态。

## 本地实际操作顺序及证据

隔离Edge打开本地`http://127.0.0.1:8765/`，通过浏览器页面操作，业务请求接到同进程实际`app.dispatch`与仅内存保存的测试数据；不是模拟按钮结果，也不是友方CloudBase。脚本为`test/import-browser.test.cjs --actual-staff`与`test/staff-actual-api-browser.cjs`。

1. 超管实际打开工作人员页，创建运营账号；随后在页内编辑姓名和手机号、停用并确认不能登录、重新启用、重置密码并确认旧密码失效。创建和换号码后手机验证仍为未验证。
2. 超管退出，运营用重置后的密码登录；本人页显示账号ID、姓名、运营角色和未验证号码，没有创建人员按钮。截图：`outputs/staff-browser/operator-own-profile.png`。
3. 运营在订单页将本地企业线下订单转为拣货中，登记实际收到30元；服务端回读100元订单为已收30元、尚欠70元，订单履约状态仍为拣货中。运营直调工作人员创建接口被服务端以`ADMIN_FORBIDDEN`拒绝。
4. 运营修改本人密码；原登录令牌失效，使用新密码再次登录。注入一次退出连接故障后，页面提示“退出未完成”，原会话仍在，允许再次点击；第二次退出成功后旧令牌被实际API以`ADMIN_SESSION_EXPIRED`拒绝。重新打开受保护工作人员页回到登录页。截图：`outputs/staff-browser/reopen-requires-login.png`。

本地页面/权限/会话测试及全部回归：`node --test test/*.test.js backend/cloudbase/functions/api/test/*.test.js`，45项通过、0失败。隔离浏览器上述脚本最后一次运行退出码0。`git -c core.safecrlf=false diff --check`通过。受影响JS单文件最大为`app.js` 666行，低于800行。规范与需求两轴独立只读审查发现的列表刷新误报、失败后表单丢内容、商品独立页临时断网误退出均已修复并回归；日志、权限和最后一个超管保护见01A服务端测试。

## 本地完成不等于上线

- 友方云环境实际账号、会话与并发保护尚未验证；本地内存事务不证明云事务行为。
- 工作人员手机号短信验证未接通。启用前须获准短信服务、签名与模板、安全凭据、测试号码及防滥用/过期/换号验证规则；当前如实保持“未验证”。
- 未部署后端或后台，未在真实小程序登录、下单或观察权限变化；实际小程序可见性另验。
- 本次本地订单和员工账号仅存在于内存夹具，不是线上业务数据。没有改变个人线或小程序前端，也没有合并企业价格规则。

回退：保留既有本地回退标签，当前修改仍未提交；不得通过重置或覆盖工作区来“回退”，如甲方不满意需先清点现有未提交文件，再按指定回退点恢复所需范围。
