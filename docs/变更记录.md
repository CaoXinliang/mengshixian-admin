# 友方后台本地变更记录

## 2026-09-24｜测试服部署与真实云验收（首次）

- 经授权首次部署到友方测试环境 `cloud1-d8gp843lt5454ada7`：api 函数仅更新代码（运行时/超时/环境变量原样），静态托管 `admin-test/` 全量更新 74 文件并清理 5 个废弃文件，补建缺失的 `order_receipts` 集合。
- 真实云验证全部通过：demo_owner 登录/退出/旧令牌重放拒绝/未登录写拒绝；SKU 上架门在真云生效（缺项上架被拒＋完整缺项清单＋无部分写入，17 断言 ALL-PASS，测试商品已归档清理）；真实云存储上传成功；托管页面全 200。地址 `https://cloud1-d8gp843lt5454ada7-1483924869.tcloudbaseapp.com/admin-test/`。
- 边界：真实支付/短信/实际小程序/长视频未验；运行时仍 16.13 未动；仓库仍未提交（部署内容=未提交工作区快照）。详见 `docs/FR-CLOUD-DEPLOY-01-测试服验收.md`。

## 2026-09-24｜表单易用性盘点与修补（恢复施工第二项）

- 盘点：三张清单——可达表单无手填内部 ID 硬伤；金额展示全部按元无填分入口；确认缺口为在售商品"专业资料"直改、SKU 表单绕过行内确认上下架、分类/轮播/首页模块/仓库保存即启用、工作流页改价、素材登记/新建版本、媒体关联启用。无批量自动决定入口。
- 修补：`app.js` 六个表单处理器按风险补确认（在售资料保存、SKU 状态变更复用行内确认、首次启用经新 `contentEnable` 分支显示对象与影响，日常编辑不打扰）；退款页装载订单列表、表格显示订单号；`admin-operations-confirm.js` 新增 `contentEnable` 分支，四个页面补加载该模块；`admin-product-workflow.js` 在售改价确认显示原价→新价；`admin-media-guide.js` 登记/新建版本确认（明确新版本影响既有引用）；`admin-tables.js` 运费行补附加运费/免运门槛元展示；`admin-forms.js` 15 处内部编号味模板文案清理（仅文案，结构未动）。
- 分工：本轮由主 Agent 与两个按文件所有权隔离的子智能体并行完成（素材板块、表格/文案板块），主 Agent 串行收口全部验证；子智能体均遵守只改分派文件、不碰 git 的边界。
- 验证：46 个测试文件全部通过（素材确认与运费/文案契约断言均先红后绿）；`git diff --check` 与全部改动文件 `node --check` 通过；默认与实际本地 API 两条隔离 Edge 链全链通过。期间发现并修复回归：分类/轮播/首页/仓库四页未加载确认模块致启用保存失败，已补加载并纳入契约测试。
- 边界：仅本地；云端事务/并发、真实云存储与长视频、短信、实际小程序未验收；营销与复杂企业采购暂缓；未提交、推送、部署或写友方云业务数据；小程序与个人线未动；残留少量隐藏模板文案留待下轮。详见 `docs/FR-FORM-USABILITY-01-本地验收.md`。

## 2026-09-24｜SKU 上架旁路最小修复（恢复施工第一项）

- 缺陷与证实：`admin.skus.setStatus` 与已有 SKU 的 `admin.skus.upsert` 改状态到 on_sale 仅查规格编码＋任意生效价格，绕过完整上架核对；旧测试 app.test.js:272 曾断言旁路成功。修复前用真实代码＋内存库执行证实（已发布商品下缺项规格一次调用即上架并对匿名顾客可见，同刻完整核对为不通过）。
- 修复：`lib/admin-catalog-actions.js` 新增 skuPreviewStore/requireSkuReviewReady，SKU 转入 on_sale 前复用 `catalog-review.read` 完整核对，缺项返回 PRODUCT_NOT_READY 与全部中文缺项及核对页引导；api/app.js 注入 catalogReview；条件齐备的 off_sale 重启用保留；`admin.demo.seedCommerce`（demoMode＋通配双门、无前端调用方）维持原状。前端 `admin-tables.js` 规格行加"核对发布"链接，`admin-operations-confirm.js` 上架确认文案明确服务端完整核对（对应测试断言同步更新）。
- 验证：新增 `sku-publish-gate.test.js` 先红后绿；app.test.js:272 改为拒绝断言＋无部分写入检查；定向 10 文件、全量 46 文件通过；`git diff --check` 与改动文件 `node --check` 通过；隔离 Edge `--actual-catalog-ready` 全链（含新增 `sku-gate-browser.cjs` 实点按钮：缺项拒绝、补齐上架、顾客可见、下架重启用）通过。
- 边界：仅本地；云端事务/并发、真实云存储与长视频、短信、实际小程序未验收；未提交、推送、部署或写友方云业务数据；小程序与个人线未动；回退为定向撤销本节所列文件改动，不覆盖其他未提交修改与既有回退标签。详见 `docs/FR-SKU-GATE-01-本地验收.md`。

## 2026-09-23｜交接前文档与当前源码对齐（本次）

- 目的：修正 API 模块地图漏列工作人员、线下收款和商品核对依赖，以及计划仍指向施工中的问题。
- 文件：`docs/API模块地图.md`按当前 `backend/cloudbase/functions/api/app.js` 和 `lib/` 的实际模块补齐职责、入口与未验边界；`OPERATIONS-USABILITY-PLAN.md`顶部标明暂停交接、未提交状态和恢复后的首个只读审查点；`docs/FR-STAFF-01-任务.md`、`docs/FR-STAFF-01A-任务.md`、`docs/FR-STAFF-01B-任务.md`修正旧“当前任务”指向；本文件补记先前已完成但未写在顶部的本地结果。
- 用户可见影响：交接者能区分当前源码、历史进度和待审线索；本次不改变后台页面、API、数据或权限。
- 验证：本次仅对照本地源码、现有验收文件及文档差异；历史自动化与浏览器结果不冒充本次重测。无小程序前端、个人线、云端业务数据、提交、推送或部署。回退方式为仅撤销本次六处文档增补，保留其他未提交改动与既有回退标签。

## 2026-09-23｜工作人员 01B/01C 本地收口（补记历史结果）

- `admin-staff-page.js`/`.css`、`admin-shell.js`及各受保护页面接入独立账号页、右上角身份和退出；服务端 `staff-accounts.js`、`staff-policy.js`、`staff-write-guard.js`承担角色、会话、权限及最后可用超管保护。详见 `docs/FR-STAFF-01B-任务.md`、`docs/FR-STAFF-01C-本地验收.md`。
- 当时记录：本地浏览器与实际本地 API 走通超管建/改/停用/重置、运营登录和获准操作、越权拒绝、退出失败重试及旧会话失效；45项自动化回归通过。手机号仍未真实短信验证，云端并发与实际小程序未验。本段是补记，不表示本次重新运行测试或上线。

## 2026-09-23｜分类图片和价格适用对象可读化（补记历史结果）

- `admin-category-guide.js`与分类页把分类图片改为按素材名称选择；`admin-pricing-targets.js`与价格列表显示可读适用对象，避免要求运维人员辨认素材或企业内部 ID。证据分别见 `docs/FR-CATEGORY-IMAGE-01-本地验收.md`、`docs/FR-PRICE-AUDIENCE-01-本地验收.md`。
- 当时浏览器定向检查、商品本地实际 API 主路径及45项回归通过；分类图片选择的页面测试使用模拟接口，云环境、长视频和真实小程序未验。本段是补记，不表示本次重新运行测试。

## 2026-09-23｜FR-STAFF-01A本地收口，转01B

- 双轴复核：需求缺口补齐；规范2项硬问题修复确认，助手独立重跑6定向文件通过。主Agent全40文件通过，语法/差异检查通过。未把云原生事务、短信或页面实操标为通过。
- 新增FR-STAFF-01B-任务.md，计划唯一入口转工作人员独立页面/统一顶栏退出。尚未改动这些UI，不称新账号页面可用。保留所有未提交变更与原回退点，无提交推送部署或线上写入。

## 2026-09-23｜FR-STAFF-01A 旧写入口退役、密码与会话

- 终段补充：staff-business-permissions.test.js用运营真实dispatch跑商品/媒体/拣货/收30元/重复幂等/审计及退款、账号、仓库、配送写入拒绝。仓库权限先失败后改admin-operations-actions.js为超管。permissions.js恢复旧finance_manager默认，app.test.js验证无显式授权旧财务不得读收款先红后绿。明确授权历史财务样本仍可收款。初始化/本人改密移至staff-accounts，组合根不再堆业务。全40文件与差异检查通过，需求轴复核闭合，规范轴复核待返回。页面与云仍未验。

- app.js删除旧角色/账号写函数并返回明确新入口提示；历史查询与登录保留。登录/getAdmin加入authVersion，本人改密走staff-write-guard原子更新密码版本和日志。staff-accounts新增超管resetPassword，停用增加会话版本，重新启用不恢复原令牌。无前端修改。
- staff-accounts.test.js旧创建入口先失败后修复；staff-password.test.js新增实际dispatch的102会话、运营越权、重置/本人改密、旧密码、重新启用和操作审计测试。app.test.js把历史角色/账号改为内存存储边界种子，保留原权限回归，不以测试继续开放旧写API。
- 定向5文件通过；全回归与双轴审查接续。未完成：工作人员页面/统一退出/浏览器实操和短信。未提交、推送、部署、写云、改个人线或小程序；保留原回退点与所有其他未提交修改。

## 2026-09-23｜FR-STAFF-01A 账号写入守卫切片

- 目的：堵住旧管理接口修改新工作人员的旁路，增加账号更新与最后可用超管保护，不改已完成商品和收款流程。
- 文件：backend/cloudbase/functions/api/lib/staff-write-guard.js新增全分页、暂存变更、共享版本及事务内会话/操作者复核；staff-accounts.js新增update、手机号未验证与安全审计；staff-policy.js改为仅自身键角色查找；api/app.js接线五条写路径并限制旧管理为超管；test/staff-accounts.test.js、staff-protection.test.js、staff-write-guard.test.js及原app.test.js验证最新权限决定。三个计划文件同步中间状态。
- 验证：旧旁路和未实现update先失败，修复后全38测试文件通过；git diff --check通过。新增模块均不足800行，API组合根661行。独立静态预审反馈已记录在01A，正式终审未完成。
- 用户影响：目前仅本地服务端能力，尚无新版账号页面或本轮浏览器验收。手机号无短信验证、管理员密码重置未开、旧角色兼容新增入口待收口；云原生事务冲突未实测，不能把内存串行事务称真实云验收。
- 安全范围与回退：蓝色多页面未改；小程序、个人线、原回退点未动。无提交推送部署或线上业务写入。保留用户已有未提交内容；后续回退须按用户明确指定的点实施，不自行重置。

## 2026-09-23｜工作人员服务端创建首切片

- 新增lib/staff-accounts.js与staff-policy.js，app.js仅接入admin.staff.create。超级管理员创建工作人员，固定super_admin/operator权限，不接受自填权限/角色ID或伪造已验证。手机号加密保存、对外脱敏且未验证；创建与audit_logs同事务写入。
- permissions.js对新staffRole按固定策略派生权限，旧账号原有权限暂保留；api-values.js安全投影新增role/手机号验证状态，不返回密码或明文手机号。
- test/staff-accounts.test.js先因路由不存在失败，再验证创建运营/角色限制/未验证手机号/重复账号/日志/实际登录与退出token失效；全36测试文件和差异检查通过。独立安全预审已派发。
- 本阶段尚缺旧管理入口适配、账号更新、共享写入守卫和最后超管保护，以及页面/短信/浏览器验收。未部署、推送提交、写线上数据或改小程序/个人线、回退点。回退本批仅撤对应模块/接线/投影增量，不覆盖既有修改。

## 2026-09-23｜商品本地主路径收口，接续工作人员服务端任务

- 独立审查确认真实业务dispatch与同数据多身份；补逐行错误/缺项、时段实际保存字段和发布日志对象/唯一性断言，完整本地浏览器再次通过。保留价格标记、云环境/小程序和并发未验限制，不宣称商品全验收或上线。
- 激活已排队FR-STAFF-01A，新增服务端模块小票据，沿父需求权限清单和用户明确测试接缝，模块设计技能约束独立账号模块、共享主文件只接线。无工作人员新功能实现声明。
- 本批仅测试和文档，无部署推送提交、线上业务数据写入、个人线/小程序或原回退点改动。

## 2026-09-23｜同一交付商品本地发布及身份报价验收

- 新增catalog-publish浏览器helper，fixture增加同内存数据库不同顾客身份的真实API入口。完整浏览器命令通过：草稿不可见、取消不发布、明确确认后个人目录/详情/素材/价格可读；2件加运费30元，超库存拒绝。
- 企业测试身份通过实际申请/审核生成，不配置企业价；企业目录可见但无价格、报价拒绝，不合并B/C规则。无身份请求价格被拒绝。只读核对小程序services接口名称，未运行或更改前端。
- 35测试文件及差异检查通过；终段独立需求复核待返回。本地测试资料与内存数据库，不代表云端/小程序验收；原开放并发与外部验证缺口保留，工作人员任务排队。
- 本批仅测试和文档，无真实业务数据写入、提交推送部署、个人线或回退点改动。

## 2026-09-23｜同商品价格库存配送跨页闭合到待发布

- 接收pricing/inventory浏览器helper并新增delivery helper，actual-catalog-ready串联同商品。页面名称选择、个人元价格、取消零写、库存10、运费5元/门槛100元与时段、分类启用均通过实际本地API核对，最后ready但仍draft。
- 首次运行修正库存helper选择器引号；再次实测发现app.js异步库存提交后event.currentTarget已空，reset抛错。最小修复改用闭包inventoryForm，完整链复跑成功；不改服务端库存计算。
- 本地合成数据，未点击发布、未证明身份可见或下单。独立复核已派发。无线上写入、提交推送部署、个人线/小程序改动或回退点修改；工作人员任务保持排队。

## 2026-09-23｜同商品图片视频页面与实际API链路通过

- 接收`test/catalog-media-browser.cjs`写入权并实际运行媒体开关：同交付商品→素材页上传PNG/WebM并登记→核对页名称选择主图/详情视频→图解码与视频播放起步通过，缺价格时UI与API均阻止发布，商品仍draft。
- 35测试文件和差异检查通过。素材明确demo/temporary，存储与预览地址为内存替身；不声称长视频、真实云存储或生产客户端分段链通过。
- 两助手接续仅独占新价格/库存浏览器helper，尚无运行结果；主Agent集成，工作人员任务保持QUEUED。未改生产代码、个人线、小程序、原回退点，无提交推送部署及线上业务写入。

## 2026-09-23｜同商品素材链本地存储接缝

- catalog内存夹具增加独立文件与预览适配器，新增`test/catalog-storage-fixture.test.js`；真实上传接口先返回未配置，接入后验证上传/登记/发布分离和历史文件不覆盖，全35测试文件及差异检查通过。
- catalog浏览器桥接小文件传输和内存临时URL，准备`--actual-catalog-media`开关；商品草稿helper返回同一productCode供跨页继续。助手仅写新media helper，尚未跑新增媒体浏览器链，不称验收完成。
- 本批仅测试设施，不改生产上传逻辑或交付模板，无部署、推送提交、云端业务写入、小程序/个人线或回退点改动。工作人员任务保持排队。

## 2026-09-23｜工作人员账号权限独立小任务排队

- 按最新要求只读核对api/app.js登录、退出、账号、角色与audit，permissions.js、access.html、admin-access-guide.js、admin-shell.js和相关测试；保留现有能力，不重做登录或扩大角色。
- 新建`docs/FR-STAFF-01-任务.md`：现状缺口、两角色逐项权限目标、手机号未验证及短信条件、模块边界、越权/注销/最后超管/审计/浏览器验收。计划列QUEUED，在商品连续验收之后执行，不改变当前唯一小任务。
- 本批仅文档，无代码/云端账号/权限变更，无提交推送部署、个人线/小程序修改；没有宣称账号功能已实现或实测。

## 2026-09-23｜同交付记录生成草稿并进入缺项核对

- `test/catalog-actual-api-browser.cjs`接入`test/catalog-draft-review-browser.cjs`，主Agent实际浏览器验证同一TEST-S-0001取消/确认生成草稿、从该行跳到核对页，缺资料时发布按钮禁用且API状态仍draft；没有编造新商品替代交付行。
- `test/order-receipts-browser.cjs`只加强刷新新文档、管理员身份、原凭据及待核对面板等待；助手完整浏览器串行3次通过，主Agent新跨页再跑通过。收款产品代码未改，保留首次失败证据。
- 同步当前计划及验收文件，下一步同商品补素材/价格库存/配送，整体链路未完成。34测试文件通过；本批测试模块均小于800行。
- 无部署、提交推送、线上数据写入、小程序/个人线改动或回退点变化。回退只撤本批测试增量，不影响原有代码。

## 2026-09-23｜跨文件编码面板归属保护

- `admin-import-mapping.js`为每次挂载建立独立owner，旧处理器不能点击、隐藏新面板或报告旧错误。`test/import-mapping-panel.test.js`先复现失败再验证通过；34测试文件与差异检查通过，独立只读复核未见相关新阻断。
- 准备`test/catalog-draft-review-browser.cjs`沿交付表首条记录继续取消/确认生成商品草稿、跳转核对页和缺项禁发布；尚未接入运行，不能声称这条跨页已通过。
- 收款刷新失败已定位测试只等待弹窗节点、未确认新文档及管理员身份的时序风险，助手仅修测试等待并重复验证中；产品收款代码未更改。
- 无模板/定价规则变更、个人线/小程序修改、提交推送部署或线上写入，原回退点不动。回退仅撤本批映射owner与对应测试，不覆盖已有工作。

## 2026-09-23｜编码重新核对期间禁止保存旧结果

- `admin-import-workflow.js`核对开始清除可保存结果并禁保存、隐藏旧预览；`admin-import-mapping.js`进行中禁再次生成，防止双击产生两套编码。保留模块和蓝色多页面结构，无模板或业务价格规则更改。
- 新增`test/import-mapping-race.test.js`用真实映射/解析/本地API先复现旧记录写入和未禁按钮，再验证修复；浏览器actual-catalog追加延迟映射响应场景通过，全33测试文件及差异检查通过。
- 首次浏览器运行因既有收款刷新form.hidden断言失败，复跑通过但原因未定；已记录并交只读诊断，不隐去失败。映射最终复核待返回，商品跨页链路未完成。
- 本地数据库替身，不代表云端或小程序验收；无提交推送部署、线上数据写入、个人线/小程序修改或回退点变更。回退本批仅撤除上述两模块状态保护及直接测试，不覆盖其他工作。

## 2026-09-23｜交付XLSX实际API草稿验收与换表保护

- 新增catalog实际API浏览器测试与空商品内存夹具，测试入口增加开关；收款夹具仅导出内存适配器。主Agent执行交付空白/演示XLSX预览、取消、保存6条草稿和去重成功，无自动商品生成或上架。
- `admin-import-workflow.js`修复快速换文件迟到覆盖；`test/import-workflow-race.test.js`真实解析/API先复现失败，再验证只保存最后选择的表，保存时禁选。32测试文件通过，完整浏览器测试通过，差异检查通过。
- 新增`docs/FR-CATALOG-FLOW-01-本地验收.md`并同步计划与运维路径。独立复核另指出映射预览等待时保存风险，尚未实测，作为下一动作，不隐去未完成项。
- 本地内存替身不代表云端验收；无提交推送部署、线上写入、个人线/小程序改动。回退点不变；撤回本次行为改动仅涉及导入状态保护及直接测试，不覆盖其他修改。

## 2026-09-23｜当前计划与历史状态分离

- 核对01C本地验收和FR-CATALOG-FLOW-01任务后，给`OPERATIONS-USABILITY-PLAN.md`增加明确的历史分界，修正旧“当前事实入口”表述；不删除历史证据，不重新实施已完成收款。
- 当前唯一小任务仍是交付XLSX经页面调用实际本地API预览与草稿。简化企业下单及线下收款保留；营销、询价、单独报价与复杂账期暂缓。
- 本批为文档修正，未改变业务代码、原回退点或线上状态；无提交推送部署、个人线/小程序修改。后续测试结果另行记录，不预报通过。

## 2026-09-23｜收款本地收口，接续交付表格连续验收

- 01C Standards/Spec两位独立审查均无阻断，明确实际业务实例与同单流水证据；修正浏览器入口过期的“全部mock”注释。没有宣称云端、小程序或真实资金完成。
- 更新01C本地验收与唯一计划入口，新增`docs/FR-CATALOG-FLOW-01-任务.md`，回到用户原要求的交付XLSX→实际API草稿→商品补齐发布链路；首先只做文件与草稿接合，不重做已完成模块。
- 本批仅测试注释、文档和任务状态，无业务/小程序/个人线改动，无提交推送部署/线上写入，原回退点不变。

## 2026-09-23｜同单实际API与浏览器收款演示通过

- 子Agent新增`test/support/receipt-flow-fixture.cjs`及直接测试，只有内存数据库替身，无云SDK；通过真实业务API创建企业身份、配置与100元线下订单。主Agent检查其数据范围为本地合成夹具，不改生产定价。
- `test/receipt-actual-api-browser.cjs`启用Runtime绑定事件，修复首次页面未得到接口响应的测试连接问题。执行`node test/import-browser.test.cjs --actual-receipts`通过：实际订单行→30元→70元→收齐，只有两条流水、重放安全、履约不变。
- 全31文件通过，差异检查通过；截图`outputs/receipt-browser/actual-api-paid.png`已查看，完整证据和云端/小程序缺口写入`docs/FR-RECEIPT-01C-本地验收.md`，运维说明同步。
- 不提交推送部署、不写线上数据、不改小程序/个人线、原回退点不动；整体验收待最终审查，不把本地成功称已上线。

## 2026-09-23｜01C同单实际API浏览器连接准备

- 新增`test/receipt-actual-api-browser.cjs`，通过隔离Edge的本机绑定调用同一个实际业务应用，不搭建或连接云服务；从真实订单表格打开面板，准备登记30元/70元、核对两条流水、重复凭据及履约不变，并生成独立截图。
- `test/import-browser.test.cjs`添加显式`--actual-receipts`运行开关，原模拟用例保持。脚本语法与差异检查通过；实际业务夹具由子Agent独占`test/support/receipt-flow-fixture.cjs`和直接测试实施，尚未返回，因此本轮未运行同单浏览器验收，不能称完成。
- 无产品代码或个人线/小程序修改，不提交推送部署或写线上数据；回退只撤回本批测试脚本与接入点，原回退点不变。

## 2026-09-23｜01B跨单恢复说明补齐

- Spec轴指出刷新后恢复原订单时，若用户点击另一单，应明确告知不是新选订单。`test/order-receipts.test.js`增加跨单恢复用例先失败；`admin-order-receipts.js`恢复后提示先处理原订单，再显示原收款信息，避免静默切换误读。
- 全30测试文件及差异检查通过；Standards轴已重新运行定向测试确认管理员id契约修复，Spec轴已通知复核两项整改。暂无云端/小程序证据，不能把mock浏览器当实际API同单闭环。
- 本批仅面板提示与直接测试、记录，无提交推送部署、线上写入、小程序/个人线改动或回退点变更。

## 2026-09-23｜真实浏览器刷新验收与管理员字段契约修正

- `test/order-receipts-browser.cjs`增加真正Page.reload：模拟登记响应中断后刷新、重开面板、不能另建、原内容原凭据重试；隔离Edge实际通过。该测试接口仍模拟，不是01C实际业务API闭环。
- 01B规范独立审查发现管理员返回字段为id而非_id；`test/order-receipts.test.js`改用后端真实cleanAdmin投影，先复现失败，`admin-order-receipts.js`修正pendingKey读取id，浏览器模拟账号也遵循真实契约。全30文件与Edge重跑通过。先前使用错误模拟字段的通过结果不能证明真实接入；本次已补契约回归。
- 01B两轴审查仍待最终收口，未宣布完整交付；同单本地API浏览器演示未完成。未改小程序/个人线或原回退点，未提交推送部署/线上业务写入。

## 2026-09-23｜收款未确认请求跨页面刷新保护

- 接收子Agent交接，收款模块/样式/直接测试写入权归还主Agent。`admin-order-receipts.js`发送前将原内容与幂等凭据保存在当前标签页sessionStorage，按管理员账号隔离；重新打开面板优先恢复待核对原订单，禁止另建登记；明确成功才清除。不存管理员令牌。
- `test/order-receipts.test.js`新增模拟刷新重建面板回归：先复现刷新后可另记，再验证原凭据重试及成功清除。测试响应修正为有效receipt._id，避免把未确认响应当成功。浏览器模拟账号补本地_id。
- 全30文件通过，差异检查通过；隔离Edge基础操作与同凭据失败重试复测通过。跨刷新本次为重建页面模块的交互测试，未把它说成实际浏览器刷新或CloudBase验收。
- 限制：sessionStorage仅当前标签页，关闭标签页、换设备、清数据后恢复未覆盖；01C实际API同单浏览器链路及01B独立审查仍待完成。不改原回退点、小程序/个人线，无提交推送部署/线上写入。

## 2026-09-23｜收款面板浏览器截图与回归

- `test/order-receipts-browser.cjs`补重试成功后累计50元断言，生成`outputs/receipt-browser/partial-receipts.png`。主Agent实际查看截图：蓝色面板显示100元总额、已收50、待收50及30/20两条记录，没有内部编号输入；小视口可滚动。
- 隔离Edge整套操作通过；全30个测试文件通过，差异检查通过；当次面板JS208行/CSS27行。截图使用明确的模拟订单，不能代替01C实际dispatch同单浏览器验收。
- 面板Agent仍在收尾，需最终接收与独立审查，特别是跨刷新未确认登记的保护。不宣布01B完整完成；不改小程序或个人线，无提交推送部署及线上写入，原回退点不变。

## 2026-09-23｜订单收款面板接入与首轮浏览器操作

- `orders.html`接入独立收款脚本、元金额工具与面板样式引用；`test/import-browser.test.cjs`调用独立收款浏览器助手。样式及面板收尾仍由子Agent进行，不宣布视觉/完整功能交付。
- `test/order-receipts-browser.cjs`隔离Edge实际验证面板挂载、填写30元、取消零请求、双击一笔、余款70元，以及模拟中断后隐藏新登记入口、使用相同金额/时间/凭据重试。已运行两轮通过，全部API为本地模拟，没有CloudBase请求。
- 主Agent读代码发现需补：保存成功后的刷新失败不能误报收款失败，成功清表单，切换订单异步旧响应隔离，未确认请求跨刷新保护；已交独占模块Agent处理，尚待复测。当前不是同一实际API订单的浏览器闭环，01C仍未完成。
- 无提交推送部署、线上数据或小程序/个人线修改；原回退点未动。回退仅本批HTML引用及测试接入，不覆盖既有成果。

## 2026-09-23｜准备独立收款浏览器验收

- 新增`test/order-receipts-browser.cjs`：复用既有隔离Edge驱动，准备订单页面板挂载、按元填写、取消零请求、重复点击仅一笔与剩余70元的UI检查。响应明确为本地模拟，不连接CloudBase。
- 当前只通过`node --check`；面板子Agent仍在实施，此助手尚未接到主浏览器脚本执行，不能称浏览器验收通过。没有页面新接入或业务代码变更。
- 不改原回退点、小程序或个人线，无提交推送部署、云数据写入。回退仅删除本批新增测试助手及记录，保留所有其他改动。

## 2026-09-23｜01B订单收款入口与按钮权限

- `admin-tables.js`：线下订单按receipts.read显示“查看收款”；履约按钮按orders.write显示，财务只读订单不再出现发货按钮。服务端鉴权不变。
- `app.js`：增加未收款/部分收款/已收齐中文映射，预留独立面板mount注入call、getAdmin、刷新订单；当前面板由子Agent实施，尚未向orders.html接入，不能据此称入口已可用。
- `test/order-receipt-entry.test.js`：先复现入口缺失与无权限按钮问题，再检查线下/微信订单、读写权限和通配权限。执行时全30个测试文件通过（含子Agent当时已写入的阶段性面板测试），不代表其完整面板验收。
- 保持蓝色多页面、原回退点与未提交修改；没有小程序/个人线改动、提交推送部署或线上业务写入。后续接页面并跑隔离浏览器，不用静态/模拟测试替代操作验收。

## 2026-09-23｜01A审查收口并进入订单收款面板

- `docs/FR-RECEIPT-01A-审查结果.md`记录独立规范/需求双轴结果与技能工作区适配限制；01A本地门槛通过，不代表云端或小程序。`test/app.test.js`补固定5000分总额、3000分首笔、2000分余款断言，28测试文件通过。
- 新增`docs/FR-RECEIPT-01B-任务.md`，总计划当前步骤切到01B，独立模块与主Agent共享文件写入边界明确。面板尚未完成，不称可操作。
- `lib/offline-receipts.js`（API目录）读取接口增加registrationBlockedReason，向界面解释取消/退款、历史未知、已收齐为何不能登记；共用取消/退款判断，写入守卫保持有效。专项用例先失败后修复，再全28文件通过。
- 用户原回退点未改；无提交推送部署、线上数据写入、小程序或个人线修改。撤回本批时仅撤回新返回字段、相关测试和本批文档，不覆盖其他未提交成果。

## 2026-09-23｜收款拦截在途退款

- 主Agent核对退款模块真实写入字段：申请阶段使用activeRefundId，未必已有refundStatus。`backend/cloudbase/functions/api/test/offline-receipts.test.js`新增负例，先复现未拒绝收款；`lib/offline-receipts.js`补activeRefundId拦截，避免在途退款期间新登记。
- 全28测试文件和差异检查通过；只用本地内存，未测试云端、小程序或真实资金。不新增线下退款业务，不改其他前端/个人线，不部署推送或线上写入。回退仅本批守卫及测试。
- 两轴审查按用户批准的本地工作区差异适配：规范轴未发现明确硬违反，提出取消保护重复判断一项非阻断维护建议；需求轴仍运行，已通知复核本修正。尚不宣布01A审查完成。

## 2026-09-23｜送达与收款双向独立接口回归

- `backend/cloudbase/functions/api/test/app.test.js`：扩展同一笔50元企业订单，收30元后依次拣货、配送、送达，每步仍为部分收款并欠20元；登记余款后保留已送达，不自动确认收货。部分收款时客户与管理员直接取消仍被拒绝。
- 前后台28文件通过，验证入口为实际本地dispatch，数据库为内存替身；不代表后台页面、小程序或云端已验收。没有修改业务实现、UI、小程序和个人线，没有推送、部署或线上写入。
- 回退只撤回本批测试断言，不回退其他整改。01A正式双轴审查与01B收款界面仍未完成。

## 2026-09-23｜财务订单入口权限与实际接口同单链路

- `backend/cloudbase/functions/api/lib/permissions.js`：默认财务角色补orders.read，不添加orders.write；存储中显式权限列表保持原样，不静默扩权。
- `backend/cloudbase/functions/api/test/app.test.js`：先复现财务无法读取订单，再验证修复及无履约写权限；追加实际orders.create创建的50元企业线下订单，登记30元与余款20元、重试不多记、拒绝直接取消已收款订单、流水两条、收齐后仍待确认。沿用既有测试价格，不修改或编造生产价格。
- 规格与总计划更新已确认状态及证据，区分01A接口审查与01C整体审查。三个子智能体只读预审完成；正式code-review技能默认已提交差异为空且缺issue-tracker适配，不能宣称通过，已请求工作区审查范围。
- 全部28测试文件通过；只有本地实际业务函数+内存存储，不是浏览器同单、友方云事务或实际小程序验收。收款界面未实现。
- 不改蓝色多页面结构、小程序和个人线，未提交/推送/部署/写云数据；回退只撤回权限新增项及本批测试、文档，不重置其他未提交修改。

## 2026-09-23｜并行预审发现并修正旧付款状态误判

- 子智能体只读预审指出：旧订单即使存在零实收字段，旧 `paid/not_required` 状态也不能证明未收款。主Agent先在 `backend/cloudbase/functions/api/test/offline-receipts.test.js` 复现（实际 unpaid、预期 unknown），再修改 `lib/offline-receipts.js`：非新版线下收款状态统一待核实，拒绝直接登记。不迁移或重写历史订单。
- 复测前后台28个测试文件通过。本轮按 tdd 技能先失败再修复；仅本地内存验收，不代表云端/小程序完成。单模块保持800行以内，无前端/个人线改动、提交、推送、部署或云业务写入。
- 预审另发现财务角色缺订单读取权限，尚待按最小权限补用例与修正；正式双轴审查、实际创建订单同单演示及登记UI均未完成。回退仅本条新增旧状态保护及对应测试，保留其他整改。

## 2026-09-23｜收款权限、故障回滚与并发边界补测

- `backend/cloudbase/functions/api/test/app.test.js`：通过实际 dispatch 验证收款读取和登记均拒绝未登录、失效会话及只有商品读取权限的账号。
- `backend/cloudbase/functions/api/test/offline-receipts.test.js`：补两笔6000分请求争用10000分余额；收款流水、订单、审计三处写入故障均整笔回滚且可重试；取消/关闭/退款中订单与非法金额、日期、方式拒绝。并发使用串行内存事务，不代表真实云并发验收。
- `OPERATIONS-USABILITY-PLAN.md`：清除顶部仍在等待确认的冲突，记录当前01A及三路只读核查分工；不把准备工作算成订单登记界面完成。
- 验证：`node --test test/*.test.js backend/cloudbase/functions/api/test/*.test.js` 28个测试文件通过；`git -c core.safecrlf=false diff --check`通过。本批增加回归测试，未改业务实现和界面。
- 未完成：实际下单开始的同单演示、订单收款界面、正式独立审查；未改小程序/个人线，未提交推送部署或写云业务数据。回退仅撤回本批测试和文档块，保留其他未提交整改。

## 2026-09-23｜收款顺序获确认，修复历史金额与非法日期

- 用户已逐项采纳账目安全、登记界面、同单演示三个步骤；规格确认，新增 FR-RECEIPT-01A 当前任务说明。保留既有代码，不重建订单和价格流程。
- `lib/offline-receipts.js`：缺实收字段的旧订单返回“历史收款待核实”状态与空金额，拒绝直接补记；不将缺字段当作零，不修改旧订单。实际收款时间严格校验日历日期、小时与时区，不再把2月30日改成3月日期接受。
- `test/offline-receipts.test.js` 先增加失败测试，运行证实旧实现把历史订单误报 unpaid；修复后前后台28个测试文件通过，差异检查通过。仍是本地内存测试，不是云环境或实际小程序验收。
- 未完成：其余01A安全用例、真实API创建同单演示、订单登记界面及独立审查。无提交推送部署、云数据写入或小程序/个人线修改。

## 2026-09-23｜待确认期间收款边界诊断

- 只读执行现有收款函数的内存探针，确认两个缺陷：历史已付款线下订单缺实收字段时可再次登记；不存在的 2 月 30 日被自动转为 3 月 2 日后接受。证据及待修要求记入收款规格草案。
- 尚未获得规格拆分/验收确认，本轮不新增业务实现，只补诊断记录。没有真实收款、云数据写入、部署、推送或个人线修改。

## 2026-09-23｜收款规格草案与验收确认

- 新增 `docs/FR-RECEIPT-01-规格草案.md`，列明实际 API/后台界面验收接缝、v1 契约、三个纵向步骤及禁止事项。状态为待确认草案，不冒称正式票据已批准或技能链完成。
- 只读核查发现历史线下订单缺实收字段时当前模块默认零，不能据此证明历史未收款；已登记为兼容性缺口，待确认后修正。尚无订单收款界面，旧支付标签也未识别新状态。
- 本轮只补计划草案和记录，无业务代码修改，无推送部署和线上数据写入。

## 2026-09-23｜简化企业下单收款恢复及任务对齐

- 修正 `OPERATIONS-USABILITY-PLAN.md`、`docs/暂缓模块开发路径.md`、`docs/普通运维操作路径.md`：唯一当前任务为 FR-RECEIPT-01，企业简化下单与线下收款恢复，复杂企业采购和营销仍暂缓。
- 已有未提交实现：新增 `lib/offline-receipts.js` 与测试，API 收款读取/登记入口、专门收款权限、订单初始待收款和金额字段、顾客侧安全字段投影、客户与后台直接取消已收款订单的拦截。收款记录及审计在事务内写入，履约状态不变；订单内界面尚未实现。新增 order_receipts 集合仍未在线创建。
- 技能证据不完整：实际使用 codebase-design 拆模块，有本地测试；正式规格/票据确认、测试先行完整证据、独立 code-review 尚未完成。不得声称已全套执行。
- 不部署、不推送、不提交、不写线上业务数据，不修改小程序和个人线，不删除既有企业资料和订单，不改变价格规则。回退按收款模块、接口、权限、订单新增字段及取消保护作为整体定向撤回，保留其他整改。

## 2026-09-23｜同商品导入至个人结算的本地 API 接续验收

- `backend/cloudbase/functions/api/test/app.test.js` 将导入后主图、分类、价格、库存的直接数据库夹具修改改成实际管理员 API 调用；上传使用仓库 PNG 字节，存储上传器仍为内存测试替身。商品两个规格保持草稿直到核对发布。
- 同商品发布后，独立个人测试身份调用价格、地址与结算报价接口，验证两个规格 19.90/29.90 元、商品合计 49.80 元、运费 5.00 元、应付 54.80 元，并验证购买 3 件超过测试可售库存 2 件时拒绝报价。
- 27 个前后台测试文件通过，差异检查通过。此为真实业务函数配内存存储的 API 集成测试，不是同一浏览器跨页面接真实本地 API，也不是云端上传或实际小程序验收。起始导入数据仍为程序化测试行，不冒称交付 XLSX 已连续送入该 API 链路。
- 本轮只改测试与记录，无业务代码、小程序、个人线变化，无提交、推送、部署及线上写入。定向撤回本节测试即可回退。

## 2026-09-23｜核对后发布按钮与运维路径说明

- `test/import-browser.test.cjs` 新增确认发布实点：取消无发布调用、双击仅一次、提交当前商品与核对凭据、成功后重读显示上架。实际运行通过；发布动作由页面模拟 API 拦截，不改变任何线上商品。
- 新增 `docs/普通运维操作路径.md`，列出六步入口、已验证边界及后续缺口，明确当前未部署且不要在可能连接真实云的后台直接试演示资料。
- 本轮只改测试及文档，无业务代码、小程序或个人线修改，无提交、推送、部署与线上写入。可定向撤回新增测试和说明文件。

## 2026-09-23｜配送区域名称选择实测与默认停用

- `admin-operation-forms.js`：新建配送区域和时段默认停用，编辑既有记录保留原状态；运营须明确选择启用。
- `test/import-browser.test.cjs` 在隔离 Edge 实际打开区域选择器，点击省、市、区县，勾选仓库，验证区域名称和编码自动生成、取消零请求、确认后提交停用状态和选中仓库。首次脚本选择器错误已修正后重跑通过，不是页面故障。
- 27 个前后台测试文件通过，隔离浏览器流程与差异检查通过；所有提交均由本地模拟接口拦截。未写线上配送设置、未部署、未提交或推送，无小程序/个人线改动。
- 回退只撤回新建默认状态和本节浏览器测试场景，保留其他操作改进。

## 2026-09-23｜配送区域和时段保存前确认

- `admin-operation-forms.js` 新增配送影响摘要，按区域、仓库名称和时间说明启用/停用影响，不展示内部编号；`app.js` 的配送区域和时段保存处理在提交前要求确认，取消即返回。
- 新增 `test/delivery-confirm.test.js` 覆盖中文名称、时间、影响及取消/确认分支；27 个前后台测试文件和差异检查通过。新增配送确认尚需浏览器实点验证，不用单元测试冒充完整页面流程。
- 后续隔离 Edge 已实际填写时段，验证取消零请求、确认摘要只含名称且提交所选仓库，运行通过。配送区域确认目前仍仅单测，不扩大浏览器验证结论。
- 不改变配送匹配算法、历史订单、页面蓝色风格，不修改小程序或个人线，不提交、推送、部署或写线上数据。回退仅撤回本节摘要函数及两处保存确认调用，保留其他工作。

## 2026-09-23｜视频预览失败、延迟恢复与实际播放验证

- `test/import-browser.test.cjs` 新增隔离 Edge 场景：返回不存在的 WebM，确认 video.error 且发布禁用；延迟返回素材地址，确认无法发布；返回浏览器本地生成的有效 WebM 后，确认按钮恢复，并调用播放观察 currentTime 大于 0.1 秒。
- 场景实际运行通过，语法和差异检查通过，测试文件仍少于 800 行。本轮只补测试，未修改业务页面或 API。
- 证据仅覆盖本地浏览器失败/恢复与播放起步，不代表完整长视频播放、真实云存储网络、大文件中断恢复或实际小程序可见性。测试未发起商品发布。
- 无提交、推送、部署、线上数据写入，无小程序或个人线修改。可定向撤回本节新增测试场景，其他工作不受影响。

## 2026-09-23｜素材预览加载完成前禁止发布

- `admin-product-review.js` 跟踪图片加载及视频元数据加载，不再把取得临时地址视为预览完成。加载中、获取地址失败或媒体加载报错均阻止发布，显示等待或重试提示；迟到的旧商品媒体事件不改变新商品状态。
- `test/import-browser.test.cjs` 在服务端模拟 ready=true 的条件下，实际请求不存在图片并验证按钮禁用；随后切换仓库有效 PNG，验证图片真实宽度和按钮恢复。全过程未点击真实发布，也未访问云存储。
- 26 个前后台测试文件及隔离 Edge 回归通过，差异检查通过。视频元数据加载不等于完整播放验收，仍要求运营试播；视频失败和慢加载专项还需补测。
- 无小程序/个人线修改，无提交、推送、部署和线上业务写入。回退定向撤回本节预览状态与浏览器场景，保留其他改动。

## 2026-09-23｜核对页价格说明中文化

- 新增 `admin-review-labels.js` 独立处理价格说明；`product-review.html` 加载模块，`admin-product-review.js` 用“个人顾客价格／公开价格／指定客户专属价格”和“小程序／网页”替代英文 scopeType/channel。内部对象编号不展示，金额按元展示；未知值明确待核实。
- 新增 `test/review-labels.test.js`，前后台共 26 个测试文件通过；隔离 Edge 核对页实际断言“¥12.50（个人顾客价格 · 仅小程序）”，接口为本地模拟。
- 不改变价格计算或企业业务，不修改蓝色样式、小程序或个人线，不提交、推送、部署、写线上数据。回退可定向移除标签模块和页面引用并恢复原价格显示，保留其他工作。

## 2026-09-23｜发布前复核已有依赖记录

- 新增 `lib/review-dependencies.js`：捕获本次发布核对读取的既有文档摘要，同一文档读取期间变化即拒绝；事务内逐个按文档重读比对，全部通过后才写入。`lib/catalog-review.js` 接入，不在云事务中使用集合查询。
- `test/app.test.js` 模拟发布事务开始前改价格、库存、运费、时段，均拒绝发布且商品状态保持原状；新增快照单测覆盖删除、修改和对象字段顺序无关性。
- 限制：只覆盖读取过的既有文档，不能发现此后新增的高优先级价格/运费或新规格；集合成员变更仍需修订号机制。大量依赖时的真实 CloudBase 事务限制和性能尚未验证，不宣称云端并发验收完成。
- 本轮仅本地 API 和测试，无 UI 外观变更，无小程序/个人线修改，不提交、推送、部署或写线上数据。回退同步撤回快照模块、发布接入及测试。

## 2026-09-23｜规格并发修改与核对版本补强

- `lib/catalog-review.js`：规格包装单位、素材用途/规格关联/排序/文件/版本纳入核对指纹，避免依赖更新时间才能发现变化。发布事务写入前逐个读取规格，复核归属、状态、编码、名称、包装及更新时间，发现变化即拒绝发布。
- `test/app.test.js` 在发布前事务入口模拟规格被下架，验证返回重新核对错误且商品状态未改；另验证不改变更新时间的包装修改也使旧核对失效。
- 本轮只补规格并发保护；价格、库存、配送等依赖仍在事务外读取，尚不能声明全部并发风险已解决。CloudBase 当前事务适配器只允许按文档读写，后续必须设计依赖快照校验，不能把普通集合查询伪装成事务查询。
- 无页面外观改变，无小程序/个人线修改，不提交、推送、部署或写线上数据。回退定向撤回本节核对模块和新增测试，保留其他工作。

## 2026-09-23｜上架核对接入配送配置匹配

- `lib/catalog-delivery-review.js` 按每个规格有货且启用的仓库，核对配送区域、适用客户运费及配送时段能否形成可用组合；复用 `commerce.js` 导出的既有运费和时段解析函数，不另造结算算法。
- `lib/catalog-review.js` 将配送缺项与结果纳入发布检查及核对指纹；`admin-product-review.js` 展示仓库、区域、时段名称，提供配送区域、运费和时段补齐入口。
- 配置检查只证明至少存在可配送组合，不代表覆盖所有顾客地址、保证任意购买数量或证明收款已接通；结算时继续检查地址、数量、库存、身份和金额。
- 24 个前后台测试文件通过；新增区域停用、运费停用、客户不适用、仓库和时段错配、无库存场景。接口测试先验证缺配送不可发布，再补进程内测试配置。未向线上写入测试配置。
- 无小程序、个人线、营销或企业采购扩展，无提交、推送、部署。回退同步撤回新模块、commerce 导出及核对页/服务端调用，保留其他修改。

## 2026-09-23｜商品库存上架核对

- 新增 `lib/catalog-stock-review.js`：按销售规格读取实际库存，只有启用仓库中正整数可售数量才通过。缺记录、零库存、非法值、仓库停用或不存在均提示补齐；不生成或填充库存。
- `lib/catalog-review.js` 合并库存缺项及核对指纹，发布前重读库存状态；`admin-product-review.js` 展示规格、仓库名称、可售数量和库存/仓库维护入口。
- 23 个前后台测试文件通过；新增库存边界测试，接口测试先确认无库存不通过，再给内存夹具添加测试库存。原配送选项测试改为确认包含目标仓库，不再假设它是唯一或首个仓库。隔离 Edge 原有流程回归通过，差异检查通过。
- 配送区域、运费、时段门槛仍待接续，不能宣称整条发布链路已经闭合。本地测试库存仅存在于测试进程内，没有写入友方云端。
- 本轮不修改小程序、个人线、营销或企业采购，不提交、推送、部署。回退需同步撤回库存模块、核对调用、页面库存区域及对应测试，保留其他既有修改。

## 2026-09-23｜个人顾客价格覆盖门槛

- 新增 `lib/catalog-price-coverage.js` 判断当前有效的整数分正价及个人小程序覆盖。`lib/catalog-review.js` 对面向个人的商品逐规格检查公开价或个人类型价格，专属客户价、企业价、网页价不能冒充通用个人小程序价格。
- 价格适用对象、渠道及商品受众纳入核对版本指纹；修改这些条件后旧核对不能发布。`admin-product-review.js` 增加个人价格覆盖状态和价格核对入口。
- 22 个前后台测试文件通过；新增单测覆盖价格范围、渠道、失效期与非法金额，接口测试验证仅网页价阻止发布和旧核对失效。语法及差异检查通过。
- 库存配送门槛仍需接续完成，不据此宣称完整上架闭环已完成。无企业采购扩展，无小程序改动、提交、推送、部署及线上数据写入。
- 回退按本节两个服务端模块、页面字段和对应测试定向撤回，保留既有工作。

## 2026-09-23｜首页推荐目标的上架状态保护

- `admin-content-guide.js`：轮播和首页模块只能按名称选择已上架商品、已启用分类；原选择失效时明确要求重新选择。
- API 新增 `lib/content-targets.js`，由 `lib/admin-catalog-actions.js` 保存时校验目标存在且启用内容的目标可用。未上架目标可以保留为停用草稿，但不能启用；不存在目标报错。
- API `app.js`：公开首页内容在分页前过滤下架商品、停用分类及不适用当前客户身份的商品目标，防止后台旧推荐继续导向不可用商品。
- 验证：21 个前后台测试文件通过，新增接口场景覆盖草稿目标拒绝、无效目标拒绝、草稿保存、上架后启用及下架后隐藏；隔离 Edge 验证商品选择不提供草稿，并选中在售商品显示核对提示。差异检查通过，本次修改文件均低于 800 行。
- 本地内存接口测试与浏览器模拟不代表真实小程序验收；未部署、未写线上数据、未修改小程序或个人线。回退时同步撤回新模块和两处调用及页面筛选，保留其他工作。

## 2026-09-23｜工作台读取失败不再显示零待办

- `app.js` 为各数据源记录读取中、成功及失败状态；`admin-overview.js` 据此显示“正在读取”或“暂不可用”，只有读取成功后才显示实际数量。失败时不声称没有订单或待办。
- `test/overview-availability.test.js` 覆盖读取中、失败、真实空结果及待拣货；`test/import-browser.test.cjs` 在隔离 Edge 实际打开首页，模拟订单接口失败，再恢复成功响应，验证错误提示和零条订单正确区分。
- 验证：16 个前端测试文件通过，隔离 Edge 流程通过，差异检查通过。浏览器使用本地模拟接口，不是友方云端可用性验证。
- 无小程序、个人线或营销模块修改，无提交、推送、部署及线上数据写入。回退时定向撤回本节两个界面模块及对应测试修改，保留其他未提交内容。

## 2026-09-23｜企业采购和营销范围暂缓、素材预览提示

- 用户明确企业采购与营销本轮暂缓。新增 `docs/暂缓模块开发路径.md` 保存恢复顺序、现有代码入口、待确认业务规则和验收路径；同步 `OPERATIONS-USABILITY-PLAN.md`。保留已有功能，不部署或启用活动。
- 正在进行的素材预览整改：`admin-media-guide.js` 增加文件加载错误提示、关闭预览停止内容及异步响应防串图；`admin-product-review.js` 显示素材版本、预览失败警告及重新读取入口，预览失败时阻止页面发布。已有本地回归 20 个测试文件及隔离 Edge 流程通过；新增预览错误场景仍需定向验证，不称完整完成。
- 现状核对发现上架检查缺库存配送及个人价格覆盖、首页目标缺上架校验、工作台加载失败可能显示零，已记入计划。营销只有拼团基础，优惠券和积分尚未实现。
- 本轮无小程序/个人线修改，无提交、推送、部署或云业务数据写入。

## 2026-09-23｜素材中断恢复与重复上传处理

- `api-client.js`、`lib/admin-media-chunks.js`：服务端返回已上传分段，客户端只补传缺失分段；完整文件校验失败时作废任务并尝试清理临时分段，下次可以重新开始；成功文件保持不变。
- `admin-media-guide.js`：上传前按文件内容校验重复；格式与所选图片/视频类型不符时阻止上传；当前页面缓存已上传文件结果，素材登记失败后重试无需再次上传；选择相同内容作为旧素材新版本时提示改选文件。
- 本地测试：分段客户端和服务端验证补传、完成文件复用、校验失败后新任务与临时文件清理。隔离 Edge 使用仓库 PNG 和浏览器生成的有效 WebM 测试视频走过上传表单；模拟登记失败重试、重复图片在上传前阻止，验证通过。
- 验证边界：文件上传及登记接口在浏览器中模拟；未把上述 PNG/WebM 上传友方云存储，未验证真实大视频网络中断或小程序播放。
- 影响与回退：仅本地友方后台上传引导和必要 API；可定向撤回本节文件修改，保留其他未提交内容。未提交、推送、部署或写云业务数据。

## 2026-09-23｜后端入口按业务拆分至 800 行以内

- API `app.js`：由 1702 行降至 641 行，保留统一组装、鉴权、公开查询及 action 路由。
- 新增 `lib/admin-catalog-actions.js`（410 行）、`lib/customer-order-actions.js`（234 行）、`lib/admin-operations-actions.js`（373 行）、`lib/api-values.js`（90 行）；原动作函数主体迁移，原有权限校验、字段投影、事务、审计和错误响应保留。
- 新增 `docs/API模块地图.md` 说明职责、依赖与后续维护入口。只读迁移规划脚本在本次迁移结束后移除。
- 验证：前后台共 20 个测试文件通过；5 个迁移文件语法检查通过；git diff --check 通过。排除第三方依赖、资源和输出后，自有 JS/CJS/HTML 未发现超过 800 行的文件。
- 用户界面及小程序源码未改，本轮不涉及云数据写入、提交、推送或部署。浏览器功能验收沿用前轮证据，本轮修改由 API 回归验证，不能据此宣称云端已更新。
- 回退必须连同入口 require、工厂组装及四个模块一起定向撤回，并保留其他未提交功能修改。

## 2026-09-23｜客户身份调整按名称选择与确认

- `admin-customer-guide.js`、`users.html`、`admin-tables.js`、前台 `app.js`：客户列表显示可读客户标识；内部用户 ID 不作为输入字段；企业归属从已启用企业中选择；提交前列出客户、身份、企业及等级，取消不发写请求。
- API `app.js`、`lib/pricing-targets.js`：复用一致的客户显示名称；增加需要 `users.read` 权限的企业候选列表。返回姓名（如有）、脱敏号码（如有）及客户显示号，不暴露 openid 或手机号密文；身份修改仍要求 `users.write`。
- 测试：前后台 20 个测试文件通过；隔离 Edge 验证客户表格显示名称、隐藏内部输入、企业名称选择、取消无请求及确认提交正确对象，通过。浏览器接口全为模拟，无云业务数据写入。
- 保留蓝色样式和多页面结构；新增引导模块小于 800 行。后端总入口超长问题仍待处理，目标未完成。
- 未提交、推送、部署；小程序和个人线未改。回退时仅撤回本批独立模块及接入点，保留其余未提交修改。

## 2026-09-23｜价格对象与旧商品表单的名称选择

- 目的：让运营人员通过名称选择价格适用对象、商品、规格和素材，取消这些入口的手工内部 ID 输入。
- `admin-pricing-targets.js`、`pricing.html`：价格适用对象异步名称选择；切换范围清空旧对象；加载失败或原对象失效时必须重新选择。公开价无需指定客户。
- `backend/cloudbase/functions/api/lib/pricing-targets.js`、API `app.js`：新增需 `pricing.read` 权限的最小字段对象列表；不返回用户 openid、手机号密文；价格写入重新校验客户、企业和等级有效性。
- `admin-product-form-guide.js`、`products.html`、前台 `app.js`：旧商品资料、规格和媒体关联表单按名称选择；规格受所选商品限制，素材受所选类型限制；切换商品清空旧规格。媒体列表表头改为素材名称。
- `admin-operation-forms.js`：名称选择标签、元金额示例及价格保存确认中的适用对象。
- 测试：前台与 API 共 20 个测试文件通过；隔离 Edge 实测价格范围切换、名称选择、商品与规格联动通过。浏览器 API 全部模拟，未验证云端上传或小程序展示。
- 保留：既有未提交修改和回退标签；未修改小程序、个人线、蓝色样式；未提交、推送、部署或写入友方云业务数据。
- 待办：后台 API 入口仍超 800 行，需继续按业务模块拆分；客户身份表单仍需名称化；真实素材及小程序链路待获准的测试环境验收。
- 回退：本批新增独立模块和接入点可定向撤回；不能直接重置整个工作区，以免覆盖其他未提交改动。
