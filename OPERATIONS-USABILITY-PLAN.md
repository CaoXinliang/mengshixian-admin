# 友方后台运维易用性整改计划（2026-09-23）

## 当前任务入口：已恢复施工（2026-09-24）

2026-09-23 的暂停交接已完成使命：无声的海已批准恢复施工，第一项即核对并修复 SKU 上架旁路，已完成并留验收记录（见下方最近本地检查点与 `docs/FR-SKU-GATE-01-本地验收.md`）。当前分支为 `codex/friend-admin-review-fixes`，回退标签为 `friend-admin-before-next-change-2026-09-23`；大量本地改动仍未提交，标签不包含后来的未提交文件。不要为了交接或施工重置、覆盖、提交或推送。代码定位见 `docs/API模块地图.md`，已完成的本地行为以各项验收记录及实际源码为准，不能单凭本计划下方历史段落判断。下一项本地工作：继续盘点运维表单中的内部编号、分金额和高风险操作，沿用已完成的商品、收款及工作人员模块。恢复施工同样不得擅自部署、推送、写入友方云业务数据、修改小程序前端或个人线。云事务/并发、真实云存储与长视频、短信验证、实际小程序仍分别待验；营销与复杂企业采购暂缓。

## 最近本地检查点（已完成成果，非当前施工指令）

2026-09-24（第三项）：经授权首次部署测试服并完成真实云验收——api 函数更新代码（配置原样）、admin-test/ 托管全量更新、补建 order_receipts 集合；登录/会话/越权、SKU 上架门（17 断言）、真实云存储上传、托管可达性全部实际通过。证据见 `docs/FR-CLOUD-DEPLOY-01-测试服验收.md`。边界：真实支付/短信/实际小程序/长视频未验；运行时仍 16.13；仓库未提交，部署内容=工作区快照。下一项候选：实际小程序端到端可见性验证（需友方小程序测试版）、真实支付退款接入前的准备清单。

2026-09-24（第二项）：表单易用性盘点收口——可达表单无手填内部 ID 硬伤、金额全部按元展示；在售商品资料直改、SKU 表单绕行确认、分类/轮播/首页/仓库首次启用、工作流页改价、素材登记/新建版本、媒体关联启用均补二次确认（`contentEnable` 分支显示对象与影响，日常编辑不打扰）；退款表显示订单号；运费表补附加运费/免运门槛元展示；15 处模板内部编号味文案清理。46 个测试文件、两条隔离 Edge 链（默认＋实际本地 API）全部通过；期间修复四页未加载确认模块的回归。证据见 `docs/FR-FORM-USABILITY-01-本地验收.md`。残留：少量隐藏模板同类文案、审计页内部定位（按设计保留）。

2026-09-24（第一项）：恢复施工第一项完成——页面单独"SKU 上架"入口与 `admin.skus.upsert` 改状态绕过完整上架核对的旁路，已由真实代码＋内存库执行证实并最小修复：SKU 转入在售前服务端复用商品核对页同一套完整核对（个人端价格覆盖、库存、配送、包装单位、主图、依赖指纹），缺项拒绝并返回中文缺项与核对页引导；条件齐备的重启用保留；旧测试中固化旁路的断言已改正。证据见 `docs/FR-SKU-GATE-01-本地验收.md`：新增失败测试先红后绿、全量测试通过、隔离 Edge 实点"上架"按钮走实际本地 API 全链通过（含顾客可见与下架重启用）。仅本地结果。

2026-09-23：分类编辑原本仍要求手填图片素材ID；已增加独立名称选择模块并让分类页加载素材，保留原关联，见`docs/FR-CATEGORY-IMAGE-01-本地验收.md`。价格管理列表原本直显客户类型与企业内部编号，现复用现有适用对象接口显示名称，见`docs/FR-PRICE-AUDIENCE-01-本地验收.md`。两项均在浏览器先复现失败后修复通过，完整商品本地实际API链与45项回归复测通过。它们不代表友方云端、长视频、小程序或整体运维整改已验收。营销依用户最新决定暂缓，只留开发路径。

## 工作人员任务历史结果：FR-STAFF-01C 本地验收已收口

2026-09-23本地结果：01A服务端、01B独立工作人员页面/统一退出及01C隔离浏览器实际本地API链路均已完成，验收入口为`docs/FR-STAFF-01C-本地验收.md`。45项自动化回归通过；浏览器走通超管建/改/停用/重启/重置、运营登录/本人资料/改密、获准订单与线下收款、越权拒绝、退出故障重试、旧会话失效与重开页面需登录。只代表本地；未部署、未推送、未写友方云业务数据。友方云端会话/并发、短信真实验证和实际小程序可见性仍单独待验。下方01A/01B接续文字已归入历史，不再是当前任务指令。

工作人员模块下一步顺序：取得用户单独发布/部署授权后才在友方测试环境复核员工会话、权限及最后超管保护；短信具备服务/签名/模板与安全条件后另做真实验证；实际小程序仅在可用的测试版本中另行验收。其他运维整改未验项目仍按各自票据推进；当前不因本地通过而自动进行任何线上动作，营销与复杂企业采购仍按既定计划暂缓。

## 历史进度（01A与此前任务的当时记录，不是当前任务指令）

本次最新：已关闭旧账号/自定义角色写入口，保留历史记录与登录；重置密码、本人改密统一守卫和authVersion会话失效，102会话测试通过。运营实际API商品/媒体/订单/收款正反权限矩阵及日志通过，修复仓库配置越权并恢复旧财务默认权限以免扩权。初始化/改密已提入账号模块。全40测试文件及差异检查通过，需求轴复核闭合，规范轴复核待返回；随后进入01B独立账号页/统一退出，再做01C浏览器。全目标仍未完成。本段覆盖下方旧接续中的“重置密码未提供/旧入口待关闭/权限矩阵未补”。

最新接续（覆盖下方旧切片快照）：已补工作人员更新及共享账号写入守卫，初始化、新建/更新工作人员、旧账号/角色写入口统一受版本检查；非超管旧管理写入口拒绝，新staff账号拒绝旧upsert修改。本地38测试文件通过，含两位超管并发降权、第二页超管、日志失败回滚及退出后在途保存拒绝。未完成：旧自定义角色新增入口退役/兼容收口、重置密码与会话规则、完整敏感操作权限矩阵及正式终审；随后进入01B页面/统一退出及01C浏览器验收。云事务冲突行为未实测，不称云并发通过。

最新状态：商品同数据本地主路径及独立需求复核已收口；真实云端/小程序、发布新增依赖并发与长视频等仍登记未验，不标FR-CATALOG-FLOW-01全部完成。当前`docs/FR-STAFF-01A-任务.md`首切片已实现运营账号创建、固定角色权限、手机号未验证/脱敏、创建日志及接口退出失效测试，36文件通过。旧管理入口兼容、账号更新与最后超管保护仍未完成，下一步先统一这些写入边界。安全预审只读，无并行写入者。整体运维整改仍未完成，营销及复杂企业采购暂缓。

用户最新决定覆盖本文件下方历史“企业采购暂缓”记录：恢复普通企业购物下单与线下收款登记，不恢复询价、单独报价和复杂账期。营销仍暂缓。下方接续记录是历史证据，不表示多个任务同时实施。

当时任务入口：`docs/FR-STAFF-01A-任务.md`，父任务及权限清单：`docs/FR-STAFF-01-任务.md`。商品证据：`docs/FR-CATALOG-FLOW-01-本地验收.md`。以下商品/收款接续描述保留当时过程，不覆盖本节最新状态。

当前检查点：交付XLSX→实际浏览器/本地API→导入草稿→明确生成商品草稿→核对页缺项禁发布已用同一交付记录跑通。快速换文件、映射等待保存、双击及旧回调隐藏新面板风险已先失败后修复，最终只读复核相关段无新阻断。34测试文件通过；收款刷新测试等待修正后助手3次浏览器通过，主Agent新跨页再跑通过。下一动作沿同一商品跨页补素材、价格、库存与配送，再核对发布及身份可见性，不能以现有缺项拦截测试代替完整链路。证据见`docs/FR-CATALOG-FLOW-01-本地验收.md`。助手写入权已归还，无并行写入者。

## 已授权排队小任务（不抢占当前工作）

本轮接续：同一交付商品已从XLSX跨页面完成素材/价格/库存/配送、核对并明确本地发布。个人接口可见且报价2×12.50+5=30元，无身份价拒绝，企业无价不套个人价且报价拒绝。35测试文件及完整浏览器通过。发布/身份段独立需求复核待返回；此前库存等复核无阻断。下一动作接收终段审查、准确登记本地与未验边界后切入FR-STAFF-01；发布新增依赖并发保护等保留后续缺口，不因主路径通过宣称整体目标完成。详见FR-CATALOG-FLOW-01本地验收，不引入真实价格或库存。

- `FR-STAFF-01` 已按排期激活，仅01A服务端为当前小任务；随后01B独立页面/统一顶栏退出，01C实际本地浏览器与权限审计验收。现状、权限清单、短信条件见父任务，尚无账号新功能完成声明。

## 历史接续记录（按当时状态保留，不作为当前指令）

### 收款01A/01B到01C准备过程

- 当前状态：01C准备实施，全部收款文件写入权已归还主Agent；面板已完成本地模拟交互、实际刷新与两轴问题复核，尚缺同一实际业务API订单的页面链路。不重做商品和配送。
- 最新审查：用户同意两助手检查，原回退点保留不动；规范轴无硬违反/一项取消守卫重复的维护建议，需求轴仍在运行。主Agent已复现并修复activeRefundId在途退款拦截缺口，28文件通过；待需求轴结果再交接01B，不再重复询问审查授权。
- 并行预审接续：旧状态误判及默认财务角色缺 orders.read 均先失败测试再修正。实际本地dispatch同一50元订单已验证收30元再收20元、重复不多记、收齐不发货；28文件通过。既有显式角色不自动扩权。正式审查尚未完成：code-review默认回退标签...HEAD为空（本轮均未提交），友方仓缺docs/agents/issue-tracker.md；已请求采用回退点对当前工作区的只读审查范围，不为审查提交代码。
- 可见结果：有权限运营从订单打开收款登记，按元填写实际收到金额、时间、方式、备注，核对后保存；显示未收款/部分收款/已收齐及历史流水。送达不改变收款，重复提交不重复累计。
- 当前代码：`lib/offline-receipts.js`（均相对于 backend/cloudbase/functions/api）、API 两个 receipts action、权限 receipts.read/write、订单初始 offline_pending、顾客订单安全字段投影、已有收款禁止直接取消。
- 验收：未登录/无权限拒绝；企业线下订单初始化待收款；部分/收齐累计准确；重复请求仅一条流水/审计；同凭据改金额拒绝；零/负/超额/异常时间拒绝；送达与收款互不修改；取消或退款订单拒绝登记；历史订单和价格规则不改；浏览器取消零请求、失败可安全重试；同一笔实际 API 创建的本地订单跑通下单至收齐。
- 顺序：接口安全测试及遗留订单兼容/审查 → 订单内收款界面/权限 → 同单浏览器与实际本地 API 演示 → 独立规范/需求双轴审查 → 回到商品完整链路剩余验收。
- 技能流程核对：codebase-design 已用于拆出收款模块；规格、验收接缝和三个步骤已获确认，01A票据已建立；本轮按 tdd 技能通过实际 dispatch 补权限回归，现有实现已通过，不虚构失败再修复。可用技能目录没有 to-spec/to-tickets/implement，不冒称调用；正式独立 code-review 尚未完成。
- 测试边界：本轮28个前后台测试文件通过，包含未登录/失效会话/无权限收款访问、并发请求防超收、三处写入故障回滚、非法输入及取消退款限制。并发采用串行内存事务替身，不证明真实云事务并发。100元→30元→70元仍以直接准备的内存订单验证，实际下单同单演示未完成。云端、小程序、真实收款均未验收。
- 架构与禁区：蓝色多页面，独立模块，单文件不超过800行；不改小程序/个人线，不提交推送部署或写线上数据，不删除企业/历史订单，不改变企业价格规则。

当时的规格入口为 `docs/FR-RECEIPT-01-规格草案.md`，票据为 `docs/FR-RECEIPT-01A-任务.md`；当时三个子智能体核查接口风险、页面接入方案、同单演示与计划一致性，尚未宣布01B完成。此段仅保留过程，最终结果以顶部指向的01C本地验收为准。

### 同商品 API 连续验收

- 程序化导入行→两规格草稿→PNG 上传登记与关联→分类/价格/库存 API→核对发布→个人价格读取→下单前报价，已使用同一内存数据集跑通；超库存报价被拒绝。
- 存储/上传替身与真实业务函数配合，不是云端验证；交付 XLSX、跨页面 UI 与该 API 连续链路尚需接合，当前不能称完整端到端验收。

### 发布按钮及操作说明验收

- 隔离 Edge 已验证取消发布零请求、确认双击只发一次及重新读取上架状态；全部发布模拟，不是云端发布。
- 普通运维路径见 `docs/普通运维操作路径.md`。完整同数据跨页真实 API 闭环及云端/小程序验收仍未完成，不能用分散模拟用例替代。

### 配送区域浏览器闭环

- 已实点省市区名称、勾选仓库、取消及确认保存，区域编码由选择器自动维护，不要求手填。新建区域和时段默认停用，编辑保留原状态。
- 隔离 Edge 及 27 个前后台测试文件通过；未写真实配送配置，不能据此声明线上可送达。

### 配送配置保存前核对

- 配送区域与时段保存前显示名称、覆盖范围及启停影响，取消不提交；27 个前后台测试文件通过。
- 新增确认流程需补浏览器取消/保存验证；名称选择沿用既有页面，不重做区域选择器。

### 视频专项本地验证

- 隔离 Edge 已通过坏视频阻止发布、素材地址延迟时禁止发布、有效 WebM 恢复并实际开始播放的检查。
- 不把短视频播放起步算成长视频完整播放或真实云端验收；完整本地业务闭环、集合新增并发边界等仍需推进。

### 素材预览加载门槛

- 核对页取得素材地址后仍等待实际图片/视频元数据加载，失败禁止发布。隔离 Edge 已实测坏图片失败及有效 PNG 恢复；26 个测试文件通过。
- 视频完整试播、失败及慢加载场景仍须补验证；此项不代表云存储或小程序验收。

### 核对页价格说明

- 价格适用对象与渠道改为中文，明确个人价、企业价、公开价及专属价的区别，不暴露内部对象编号。
- 26 个测试文件及隔离浏览器中文价格显示断言通过；不改变结算规则。发布依赖集合新增并发、完整流程验收等剩余事项仍未完成。

### 已有依赖的事务复核

- 发布在事务内重读本次核对捕获的既有价格、库存、配送、商品等文档，发现变化拒绝写入；内存接口测试覆盖改价、清库存、停运费和时段。
- 仍需处理集合新增记录的并发检测，并验证云事务容量与性能；现有文档快照不能代替这些验收。

### 发布并发边界接续

- 规格在事务内再次读取校验，核对后被下架或修改时阻止发布；包装及素材关联变化纳入核对版本。
- 价格、库存、配送依赖的事务内一致性仍未闭合，需继续处理；不得将事务外重读描述为完整并发保护。

### 配送核对接续结果

- 有库存仓库必须能匹配实际区域、适用客户运费和配送时段，才满足该规格的配送门槛；页面展示名称与补齐入口。
- 24 个前后台测试文件通过，复用服务端实际下单匹配规则。仅验证配置组合存在，不声称所有地址都能送达或支付退款已接通。
- 后续继续核对完整本地运维流程与剩余权限、预览失败及发布并发边界；真实云环境和实际小程序验收仍未执行。

### 库存门槛接续结果

- 已按规格核对启用仓库中的真实可售库存；缺失、零或非法数量阻止上架，核对页提供库存与仓库维护入口。
- 23 个前后台测试文件及隔离浏览器回归通过；仅本地测试，未写云库存。
- 下一项为配送区域、运费与时段是否共同可用的核对；不能以库存检查代替配送完成。

### 个人价格覆盖接续结果

- 上架核对逐规格检查个人小程序可用的公开价或个人顾客价；仅企业、客户专属或网页价不能通过个人价格门槛。
- 价格范围与渠道变化使旧核对失效，核对页显示覆盖状态及补齐入口；22 个前后台测试文件通过。
- 尚需补库存配送核对，继续保持未部署、未做真实小程序验收的状态说明。

### 首页推荐目标保护接续结果

- 选择列表只提供在售商品与启用分类；服务端拒绝启用指向不可用目标的内容，允许已有目标保留为停用草稿。
- 顾客首页读取时过滤下架、停用或身份不适用目标；21 个测试文件及隔离浏览器验证通过，尚未部署和实际小程序验证。
- 继续补商品上架门槛的价格覆盖与库存配送核对，不将营销及企业采购重新加入开发范围。

### 工作台异常提示验证结果

- 工作台数据源区分读取中、成功、失败；失败显示“暂不可用”，不再把未读取到数据当作零待办。
- 16 个前端测试文件与隔离 Edge 的接口失败／恢复场景通过；仅本地模拟，未验证真实云端。
- 后续仍需补上架门槛的个人价格覆盖、库存配送校验及首页跳转目标校验；营销与企业采购按记录暂缓。

范围：仅友方管理后台及其必要的 API 契约。本地实现与验证，不提交、推送、部署或写云端业务数据；不改小程序和个人线。保留原蓝色风格、多页面结构及既有未提交修改，单文件不超过 800 行。

## 最新范围修订：企业采购暂缓

营销模块也已按用户最新决定暂缓开发；保留已有拼团，不扩展优惠活动、优惠券、积分与优惠叠加。企业采购与营销均不作为本轮完成条件。详细恢复顺序、现有代码入口及前置业务决定见 `docs/暂缓模块开发路径.md`。

用户本轮明确暂缓企业采购：企业审核后的专属价格、询价、账期、应收、收款，不作为本轮开发目标和完成条件。已有代码保留；不继续扩展企业采购闭环。既有企业审核的误操作防护保留。

用户提供的完整工作日故事用于现状对照；优惠券、积分、积分商城、通用优惠叠加、司机作业与财务对账须如实标注未实现，不把此次状态核对自动视为这些新业务的实施授权。

## 工作日故事核对新发现

- 现有核对发布仅校验分类、编码、规格、主图及存在有效价格，未纳入库存、配送和顾客身份价格覆盖，不得宣称营业总检查已完成。
- 首页内容选择候选未限已上架商品，服务端也未校验目标商品上架状态；下架后首页联动尚未闭合。
- 工作台列表读取失败时将对应数据设为空数组，统计卡可能展示 0，不能当作没有待办。低库存、视频失败、配送异常和财务待办尚无完整聚合。
- 优惠券、积分及积分兑换、通用活动叠加和预约规则版本尚未实现；现有拼团不是通用优惠活动模块。

## 阶段 1：先防误操作

- 移除“批量审核并启用全部草稿”的直达入口。批量行为最多生成草稿，不自动启用分类、规格或商品。
- 企业审核、退款审核、订单状态变更显示具体对象、影响和二次确认；保留服务端权限与状态校验。
- 验收：未经核对不会发布缺价格/图片商品；取消确认不发写请求；原有定向测试通过。

## 阶段 2：普通运维可用的导入

- 提供可下载的表格模板与本地文件选择。解析后先显示商品、分类、规格、重复项、缺失字段和行号，不在选文件时写云端。
- 用户确认后仅分批保存到待审核草稿；显示成功、跳过与失败数，并可从草稿继续补资料。
- 完成导入不等于上架。价格、素材、分类和规格未核对前不得批量发布。
- 验收：无需了解 JSON 或本地代码路径；错误行可定位；重复文件重试不生成重复商品；导入确认前云端无写入。

### 2026-09-23 当前落实与新增验收

- 商品编码对应 `products.spuCode`，规格编码对应 `product_skus.skuCode`；名称和文件名仅供阅读及来源追溯，不做唯一关联。一个商品编码可对应多个规格编码，跨商品规格编码冲突必须逐行报错。
- 交付 `outputs/01a0cd53-41af-7eb2-824f-c1e8b42fa25d/商品导入空白模板.xlsx` 和取自 `backend/data/product-staging.json` 的本地测试演示表。仅前六列被当前导入程序读取，说明页列明价格、库存、产地、图片、视频不在此表导入；演示编码不是甲方正式编码。
- 无编码行在预览中可选择新建业务编码、绑定现有商品或沿用前一行商品编码，并下载带编码表。服务端仍以编码复核；不能靠名称自动合并。
- 现已在隔离 Edge 浏览器中用交付的演示 XLSX 走过 6 行解析、预览和模拟“仅保存草稿”，未对友方云端写入业务数据。空白 XLSX 未填写时不能保存草稿。仍须在获准的测试环境用真实账号、真实素材和价格复测。

## 阶段 3：闭合商品与内容操作

- 导入商品在同一商品流程中完成分类、图片、规格状态、价格核对与上架。
- 轮播和首页模块按素材名称及可读目标选择，保存前预览；新建默认不发布。
- 素材库修复名称搜索；大文件上传给出运维可执行路径，不要求手填 CloudBase 文件 ID。
- 验收：普通运营任务不复制内部 ID；预览与实际提交对象一致。

### 商品素材与发布边界

- 素材先入 `media_assets`，再以商品编码／可选规格编码写 `product_media`；主图还更新 `products.coverMediaId`。文件名仅作人工查找，不能单独决定归属。
- 独立 `product-review.html` 展示名称、分类、客户范围、各规格价格、主图、详情图、视频及缺项；`admin.products.publishReviewed` 在提交时复核。旧批量上架入口已拒绝调用。
- 大量素材“匹配清单＋批量上传”适合复用现有素材库与商品媒体关系，但必须追加批次预览、人工改正、类型和规格校验、部分失败恢复及批次审计。本阶段无真实运行的完整批量入口；列为下一阶段，不以静态表格冒充完成。
- 大视频分块上传目前仅本地代码与单测通过；友方云环境未部署／未验证，不能声称已可用于生产。

## 日常资料录入方式盘点

| 资料 | 当前合适方式 | 约束 |
| --- | --- | --- |
| 商品基础资料和销售规格 | 专用 Excel 导入草稿，后续商品编辑 | 只接收模板六列，编码关联；不导入价格、库存或媒体 |
| 商品图片、视频、轮播素材 | 素材库文件上传，再在商品核对或轮播页面关联 | 保留历史版本；不得用文件名自动归属 |
| 分类 | 分类管理表单 | 分类树与启用状态需人工核对，暂不批量表格 |
| B/C 价格 | 商品编辑公开价、价格规则表单 | 服务端裁决；企业特价不混进商品表 |
| 库存 | 库存管理受控调整 | 数量变更有流水，不通过商品表覆盖 |
| 轮播与首页内容 | 对应内容页面表单，选素材和目标 | 新建默认不发布 |
| 配送区域、运费、时段 | 对应页面表单 | 金额与区域匹配需逐项核对 |
| 企业审核、订单流转、退款、发布／下架 | 页面查看对象后单项确认 | 不提供批量表格自动决定入口 |

## 阶段 4：金额与专门配置

- 价格、拼团和运费按“元”输入并在提交前显示结果，服务端继续按整数分存储和裁决。
- 配送和角色管理的专业设置只在对应权限下出现，名称选择替代人工复制 ID；不放宽权限。
- 验收：金额转换有边界测试；失败与取消不产生部分写入；权限不足不能通过界面绕过。

每阶段完成后运行定向测试、全量前端/API 测试、语法和差异检查，并记录真实浏览器或模拟数据验收边界。未完成的阶段不标记为可用。

### 2026-09-23 名称选择接续结果

- 价格对象增加个人／企业类型、企业名称、客户可读标识、已有价格等级选择；价格服务端校验对象有效性，专用读取接口检查 pricing.read 权限并限制返回字段。
- 旧商品资料、规格、媒体关联表单增加名称选择；选择商品限制规格范围，素材类型限制候选素材。切换上级选择会清空旧关联。
- 隔离 Edge 实际点击验证通过；API 和前台共 20 个测试文件通过。所有浏览器业务请求使用本地模拟，不能据此宣称友方云环境已可用。
- 下一项：客户身份表单名称化、旧入口及大于 800 行模块继续清理；云端与真实小程序验证仍未执行。

### 客户身份调整接续结果

- 客户列表和调整表单已改为可读客户标识及企业名称选择，隐藏内部用户编号输入；修改身份、企业归属、价格等级须逐项确认。
- 客户企业列表接口独立使用 users.read 权限；身份写入仍使用 users.write。取消确认无写请求、明确确认提交所选客户及企业，已由隔离浏览器验证。
- 下一项为超长 API 模块拆分、其余旧入口复核和素材完整链路的本地验证；不把模拟验证计为云端或小程序上线验收。

### 后端模块拆分接续结果

- API 入口已从 1702 行拆至 641 行；商品素材、顾客交易、后台运营动作分属独立模块，共用值处理单独集中。文件定位见 `docs/API模块地图.md`。
- 自有 JS/CJS/HTML（不含第三方依赖、静态资源及输出）均小于等于 800 行；20 个前后台测试文件、语法与差异检查通过。
- 下一项继续素材完整链路本地验证与剩余旧操作入口逐项核对；未部署，不计为线上完成。

### 素材重试接续结果

- 上传前内容校验识别重复；登记失败后当前页面重试复用上传结果；大视频断点仅补传缺失分段；文件校验失败可重新建立任务。
- 隔离浏览器使用实际 PNG 与本地生成的 WebM 完成表单、失败恢复及重复提示验证。分段测试使用字节夹具，不代表真实网络或云存储验收。
- 下一项：核对素材版本切换、预览失败状态及导入草稿到核对发布的本地完整链路；真实云端操作仍禁止。
